# 浏览器窗口大小调整说明

## 问题解决

如果您觉得浏览器窗口太小，无法正常操作登录，现在程序已经优化为：

**登录阶段**：使用正常大小的窗口（1200x800），方便您操作登录
**运行阶段**：切换到后台模式，使用较小的窗口（400x300）

## 配置说明

### 1. 登录窗口配置

在 `咖啡订单监控.py` 文件的开头部分，找到以下配置：

```python
# ==== 窗口配置 ====
# 后台窗口位置和大小配置（可根据需要调整）
BACKGROUND_WINDOW_X = 1600  # 窗口X坐标
BACKGROUND_WINDOW_Y = 800   # 窗口Y坐标
BACKGROUND_WINDOW_WIDTH = 400   # 窗口宽度
BACKGROUND_WINDOW_HEIGHT = 300  # 窗口高度
MINIMIZE_MONITOR_INTERVAL = 2   # 监控间隔（秒）

# 登录阶段窗口配置（正常大小，方便操作）
LOGIN_WINDOW_X = 100  # 登录窗口X坐标
LOGIN_WINDOW_Y = 100  # 登录窗口Y坐标
LOGIN_WINDOW_WIDTH = 1200  # 登录窗口宽度
LOGIN_WINDOW_HEIGHT = 800  # 登录窗口高度
```

### 2. 调整参数

#### 登录窗口大小（推荐保持较大）
- `LOGIN_WINDOW_WIDTH`: 登录窗口宽度（像素）
- `LOGIN_WINDOW_HEIGHT`: 登录窗口高度（像素）

**推荐设置：**
- 标准登录：`1200 x 800`（当前设置）
- 大屏幕登录：`1400 x 900`
- 小屏幕登录：`1000 x 700`

#### 登录窗口位置
- `LOGIN_WINDOW_X`: 登录窗口左上角的X坐标
- `LOGIN_WINDOW_Y`: 登录窗口左上角的Y坐标

**推荐设置：**
- 屏幕中央：`100, 100`（当前设置）
- 屏幕左上角：`0, 0`
- 屏幕右上角：`200, 100`

#### 后台窗口大小（运行阶段）
- `BACKGROUND_WINDOW_WIDTH`: 后台窗口宽度（像素）
- `BACKGROUND_WINDOW_HEIGHT`: 后台窗口高度（像素）

**推荐设置：**
- 小窗口：`400 x 300`（当前设置）
- 中等窗口：`600 x 450`
- 大窗口：`800 x 600`

### 3. 示例配置

#### 大屏幕配置
```python
# 登录窗口（大屏幕）
LOGIN_WINDOW_X = 50
LOGIN_WINDOW_Y = 50
LOGIN_WINDOW_WIDTH = 1400
LOGIN_WINDOW_HEIGHT = 900

# 后台窗口（适中）
BACKGROUND_WINDOW_X = 1600
BACKGROUND_WINDOW_Y = 800
BACKGROUND_WINDOW_WIDTH = 600
BACKGROUND_WINDOW_HEIGHT = 450
```

#### 小屏幕配置
```python
# 登录窗口（小屏幕）
LOGIN_WINDOW_X = 0
LOGIN_WINDOW_Y = 0
LOGIN_WINDOW_WIDTH = 1000
LOGIN_WINDOW_HEIGHT = 700

# 后台窗口（小）
BACKGROUND_WINDOW_X = 1200
BACKGROUND_WINDOW_Y = 600
BACKGROUND_WINDOW_WIDTH = 400
BACKGROUND_WINDOW_HEIGHT = 300
```

### 4. 使用流程

1. **启动程序**：程序会创建正常大小的登录窗口
2. **完成登录**：在正常大小的窗口中完成登录操作
3. **确认登录**：输入"已登录"后，窗口自动切换到后台模式
4. **后台运行**：程序在后台继续监控，不会干扰您的工作

### 5. 保存并重启

修改配置后，保存文件并重启程序：

```bash
python "咖啡订单监控.py"
```

## 注意事项

1. **登录阶段**：窗口保持正常大小，方便操作
2. **运行阶段**：窗口自动切换到后台，不干扰工作
3. **屏幕分辨率**：确保窗口位置不会超出屏幕范围
4. **性能影响**：后台窗口越小，资源占用越少

## 故障排除

### 登录窗口仍然太小
- 检查 `LOGIN_WINDOW_WIDTH` 和 `LOGIN_WINDOW_HEIGHT` 设置
- 确保数值足够大（建议至少1000x700）
- 重启程序

### 登录后窗口没有切换到后台
- 检查是否输入了"已登录"
- 查看控制台输出信息
- 检查 `BACKGROUND_WINDOW_*` 配置

### 窗口位置不对
- 检查屏幕分辨率
- 调整X和Y坐标值
- 确保坐标值在屏幕范围内

### 程序无法启动
- 检查Python语法错误
- 确保配置格式正确
- 查看错误日志 