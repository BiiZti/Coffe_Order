# 门店编号设置功能说明

## 功能概述

程序现在支持自动设置门店编号，在进入外卖订单管理页面后，会自动点击门店编号输入框并输入指定的门店编号。

**当前状态：调试模式**
- 窗口保持可见，方便观察程序运行情况
- 门店编号：`s100023(邮政平台产品)`
- 调试完成后可恢复后台运行模式

## 配置说明

### 1. 门店编号配置

在 `咖啡订单监控.py` 文件的开头部分，找到以下配置：

```python
# ==== 门店配置 ====
STORE_NUMBER = "s100023(邮政平台产品)"  # 门店编号（邮政平台产品）
```

### 2. 修改门店编号

如果需要修改门店编号，只需要更改 `STORE_NUMBER` 的值：

```python
# 示例：修改为其他门店编号
STORE_NUMBER = "s100024(其他平台产品)"  # 其他门店编号
```

## 工作流程

### 1. 自动设置流程

1. **进入外卖订单管理页面**
2. **等待页面加载完成**
3. **查找门店编号输入框**
4. **点击输入框**
5. **清空现有内容**
6. **输入门店编号名称**
7. **等待下拉选项出现**
8. **点击下拉选项确认选择**
9. **继续后续操作（查询、导出等）**

### 2. 错误处理

- 如果找不到门店编号输入框，程序会重试
- 如果设置失败，会记录错误日志
- 程序会在30秒后自动重试

## 技术实现

### 1. 元素定位

程序使用以下XPath定位门店编号输入框：

```python
# 等待门店编号输入框出现
store_input = WebDriverWait(driver, 20).until(
    EC.element_to_be_clickable((By.XPATH, "//input[@placeholder='请选择门店编号' and @class='el-input__inner']"))
)
```

### 2. 操作步骤

```python
# 点击输入框
driver.execute_script("arguments[0].click();", store_input)
time.sleep(1)

# 清空输入框（如果有内容的话）
store_input.clear()
time.sleep(0.5)

# 直接输入门店编号名称
store_input.send_keys(STORE_NUMBER)
time.sleep(2)  # 等待下拉选项出现

# 查找并点击下拉选项中的门店编号
store_option = WebDriverWait(driver, 10).until(
    EC.element_to_be_clickable((By.XPATH, f"//li[contains(@class, 'el-select-dropdown__item')]//span[text()='{STORE_NUMBER}']"))
)

# 点击选项确认选择
driver.execute_script("arguments[0].click();", store_option)
time.sleep(1)
```

## 使用说明

### 1. 启动程序

```bash
python "咖啡订单监控.py"
```

### 2. 观察日志

程序运行时会显示门店编号设置的状态：

```
🏪 设置门店编号...
✅ 已设置门店编号: s100023(邮政平台产品)
```

### 3. 故障排除

如果门店编号设置失败，检查：

1. **页面是否正确加载**
2. **门店编号输入框是否存在**
3. **网络连接是否正常**
4. **登录状态是否有效**

## 注意事项

1. **门店编号格式**：确保门店编号格式正确
2. **页面加载**：确保外卖订单管理页面完全加载
3. **权限问题**：确保有权限访问该门店的数据
4. **网络稳定**：确保网络连接稳定

## 扩展功能

### 1. 多门店支持

如果需要支持多个门店，可以扩展配置：

```python
# 多门店配置示例
STORE_NUMBERS = ["s100023", "s100024", "s100025"]
```

### 2. 动态门店选择

可以根据时间或其他条件动态选择门店：

```python
# 动态门店选择示例
import datetime
if datetime.datetime.now().hour < 12:
    STORE_NUMBER = "s100023"  # 上午使用门店1
else:
    STORE_NUMBER = "s100024"  # 下午使用门店2
```

## 故障排除

### 1. 门店编号设置失败

**可能原因：**
- 页面未完全加载
- 元素定位失败
- 网络连接问题

**解决方案：**
- 检查网络连接
- 重启程序
- 检查门店编号是否正确

### 2. 输入框无法点击

**可能原因：**
- 元素被其他元素遮挡
- 页面JavaScript错误
- 浏览器兼容性问题
- 下拉选项未正确展开

**解决方案：**
- 等待页面完全加载
- 检查浏览器版本
- 清除浏览器缓存
- 确保下拉选项已展开并正确选择

### 3. 门店编号输入后无效

**可能原因：**
- 门店编号格式错误
- 权限不足
- 系统限制
- **未点击下拉选项确认选择**

**解决方案：**
- 验证门店编号格式
- 检查用户权限
- 联系系统管理员
- **确保点击了下拉选项进行确认** 