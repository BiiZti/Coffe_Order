# 多商品订单处理说明

## 问题描述

当用户购买多个饮品时，原始数据中每个商品占一行，但订单编号只在第一行显示，导致程序只读取到第一个商品的信息，忽略了后续商品。

## 数据结构分析

### 原始Excel文件结构

```
订单编号: W1202507291537425965
商品1: 西柚泡泡（冰）    份数: 1    单价: 8.00    合计: 8.00
商品2: 西柚泡泡（常温）  份数: 1    单价: 8.00    合计: 8.00
商品3: 美式（冰）        份数: 2    单价: 6.00    合计: 12.00
```

### 数据列映射

- **订单编号**: 第4列（只在第一行有值）
- **商品名称**: Unnamed: 39列
- **份数**: Unnamed: 40列
- **实际支付金额**: 第26列（只在有订单编号的行有值）

## 解决方案

### 1. 订单编号追踪

```python
current_order_number = None

for index, row in df.iterrows():
    # 获取订单编号
    order_number = row.get('订单编号')
    if pd.notna(order_number) and str(order_number).strip():
        current_order_number = str(order_number).strip()
    
    # 如果没有当前订单编号，跳过
    if not current_order_number:
        continue
```

### 2. 多商品累加处理

```python
# 处理商品名称数据
if pd.notna(product_name) and str(product_name).strip() != '商品名称' and str(product_name).strip():
    # 如果订单已有商品，追加新商品
    if current_order_number in product_mapping:
        existing_products = product_mapping[current_order_number]
        if isinstance(existing_products, str):
            existing_products = [existing_products]
        existing_products.append(str(product_name).strip())
        product_mapping[current_order_number] = existing_products
    else:
        product_mapping[current_order_number] = [str(product_name).strip()]
```

### 3. 份数累加处理

```python
# 处理份数数据
if pd.notna(quantity) and str(quantity).strip() != '份数':
    try:
        quantity_int = int(quantity)
        # 如果订单已有份数，累加新份数
        if current_order_number in quantity_mapping:
            quantity_mapping[current_order_number] += quantity_int
        else:
            quantity_mapping[current_order_number] = quantity_int
    except (ValueError, TypeError):
        print(f"⚠️ 订单{current_order_number}的份数格式错误: {quantity}")
```

### 4. 前端显示处理

```python
# 处理多商品显示
if isinstance(product_name, list):
    product_display = ' + '.join(product_name)
    print(f"📦 订单{order_id}的多商品: {product_display}")
else:
    product_display = product_name
```

## 处理结果

### 处理前

| 订单编号 | 商品名称 | 份数 | 实际价格 |
|----------|----------|------|----------|
| W1202507291537425965 | 西柚泡泡（冰） | 1 | ¥8.00 |
| (空) | 西柚泡泡（常温） | 1 | (空) |
| (空) | 美式（冰） | 2 | (空) |

### 处理后

| 订单编号 | 商品名称 | 份数 | 实际价格 |
|----------|----------|------|----------|
| W1202507291537425965 | 西柚泡泡（冰） + 西柚泡泡（常温） + 美式（冰） | 4 | ¥28.00 |

## 技术实现细节

### 1. 数据读取逻辑

1. **逐行读取**: 按行读取Excel数据
2. **订单编号追踪**: 记录当前处理的订单编号
3. **商品累加**: 将同一订单的多个商品合并
4. **份数累加**: 将同一订单的多个份数累加
5. **价格获取**: 从有订单编号的行获取实际支付金额

### 2. 数据结构

```python
# 商品映射结构
product_mapping = {
    'W1202507291537425965': ['西柚泡泡（冰）', '西柚泡泡（常温）', '美式（冰）'],
    'W1202507291407074982': ['拿铁咖啡']
}

# 份数映射结构
quantity_mapping = {
    'W1202507291537425965': 4,  # 1+1+2
    'W1202507291407074982': 1
}
```

### 3. 前端显示

- **多商品**: 用 " + " 连接多个商品名称
- **总份数**: 显示所有商品份数的总和
- **实际价格**: 显示订单的总实际支付金额

## 错误处理

### 1. 数据格式错误

- **份数格式错误**: 记录警告并使用默认值
- **商品名称为空**: 跳过该行数据
- **订单编号缺失**: 跳过该行数据

### 2. 边界情况

- **单商品订单**: 正常处理，商品名称为字符串
- **多商品订单**: 商品名称为列表，前端用 " + " 连接
- **空订单**: 跳过处理

## 性能优化

### 1. 内存使用

- **增量处理**: 逐行处理，避免一次性加载所有数据
- **映射优化**: 使用字典映射，提高查找效率

### 2. 处理速度

- **跳过空行**: 快速跳过无效数据行
- **条件判断**: 减少不必要的计算

## 日志输出

### 处理过程日志

```
📦 订单W1202507291537425965的商品名称: 西柚泡泡（冰）
📦 订单W1202507291537425965的商品名称: 西柚泡泡（常温）
📦 订单W1202507291537425965的商品名称: 美式（冰）
📊 订单W1202507291537425965的份数: 1 (累计: 1)
📊 订单W1202507291537425965的份数: 1 (累计: 2)
📊 订单W1202507291537425965的份数: 2 (累计: 4)
📦 订单W1202507291537425965的多商品: 西柚泡泡（冰） + 西柚泡泡（常温） + 美式（冰）
📊 订单W1202507291537425965的总份数: 4
```

## 总结

通过改进数据读取逻辑，程序现在能够正确处理多商品订单：

1. **完整信息**: 显示订单的所有商品
2. **准确份数**: 显示所有商品的总份数
3. **正确价格**: 显示订单的实际支付金额
4. **美观显示**: 前端用 " + " 连接多商品名称

这解决了之前只显示第一个商品的问题，为用户提供了完整的订单信息。 