# 数据检测更新速度优化

## 优化内容

### 1. Flask应用数据刷新间隔
**文件**: `CMCC_Coffe/config.py`
**修改**: `DATA_REFRESH_INTERVAL = 60` → `DATA_REFRESH_INTERVAL = 30`
**效果**: 前端应用的数据刷新间隔从60秒缩短到30秒

### 2. 咖啡订单监控程序检测间隔
**文件**: `咖啡订单监控.py`
**修改**: `root.after(60000, do_check)` → `root.after(30000, do_check)`
**效果**: 咖啡订单监控的检测间隔从60秒缩短到30秒

### 3. 前端处理等待时间（关键优化）
**文件**: `咖啡订单监控.py`
**修改**: `wait_for_frontend_processing(timeout=300)` → `wait_for_frontend_processing(timeout=30)`
**效果**: 前端处理等待时间从5分钟缩短到30秒（这是主要的性能瓶颈）

## 优化效果

### 优化前
- Flask应用数据刷新: 60秒
- 咖啡订单监控检测: 60秒
- 前端处理等待: 300秒（5分钟）
- 总体响应时间: 最多6分钟

### 优化后
- Flask应用数据刷新: 30秒
- 咖啡订单监控检测: 30秒
- 前端处理等待: 30秒
- 总体响应时间: 最多30秒

## 性能影响

### 优点
1. **响应速度大幅提升**: 总体响应时间从6分钟缩短到30秒
2. **用户体验显著改善**: 新订单能更快显示在前端
3. **实时性大幅增强**: 从6分钟延迟缩短到30秒
4. **关键瓶颈解决**: 解决了前端处理等待时间过长的问题

### 注意事项
1. **系统资源**: 更频繁的检测会消耗更多CPU和内存
2. **网络请求**: 更频繁的Excel文件读取
3. **磁盘I/O**: 更频繁的文件操作

## 配置说明

### Flask应用配置
```python
# CMCC_Coffe/config.py
DATA_REFRESH_INTERVAL = 30  # 秒
```

### 咖啡订单监控配置
```python
# 咖啡订单监控.py
root.after(30000, do_check)  # 30秒间隔
```

## 如果需要进一步调整

### 更快的响应（15秒）
```python
DATA_REFRESH_INTERVAL = 15  # 秒
root.after(15000, do_check)  # 15秒间隔
```

### 更慢的响应（60秒）
```python
DATA_REFRESH_INTERVAL = 60  # 秒
root.after(60000, do_check)  # 60秒间隔
```

## 监控建议

1. **观察系统性能**: 如果系统负载过高，可以适当增加间隔
2. **检查网络状况**: 确保网络稳定，避免频繁请求失败
3. **监控磁盘使用**: 确保有足够的磁盘空间存储日志

## 相关文件

- `CMCC_Coffe/config.py` - Flask应用配置
- `咖啡订单监控.py` - 咖啡订单监控程序
- `CMCC_Coffe/app.py` - Flask应用主文件（使用配置中的刷新间隔） 